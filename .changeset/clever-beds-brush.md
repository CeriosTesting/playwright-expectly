---
"@cerios/playwright-expectly": minor
---

Add `toContainObjectMatching` matcher for object arrays

Asserts that an array contains at least one object matching a given partial object. Supports typed arrays, negation, and an `allowMultiple` option to enforce exactly one match.
